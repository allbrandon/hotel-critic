import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "hotelcritic - AI-Verified Hotel Reviews",
  description:
    "Get the real story behind hotel ratings. AI-powered analysis filters fake reviews and delivers a True Score you can trust.",
  keywords: ["hotel reviews", "travel", "AI", "fake review detection"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
